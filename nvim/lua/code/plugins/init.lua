-- Este arquivo pode ser usado para definir plugins que rodam *apenas* no ambiente VSCode.
-- Por enquanto, ele está vazio e apenas retorna uma tabela vazia.

return {
	print("🚀 - [CODE] - PLUGINS LOADED"),
}
